apiVersion: maestro.i-am-bee.com/v1alpha1
kind: Workflow
metadata:
  name: beetest-deployment
  labels:
    app: testapp
spec:
  template:
    agents:
      - beetest
    prompt: Welcome
    steps:
      - name: begin
        agent: beetest
