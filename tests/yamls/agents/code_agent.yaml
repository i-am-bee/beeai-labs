apiVersion: maestro/v1alpha1
kind: Agent
metadata:
  name: code
  labels:
    app: slack-example
spec:
  framework: code
  mode: local
  description: code agnet
  instructions: execute python code
  code: |
    print(input[0])
    output["answer"] = "Hi!"
    